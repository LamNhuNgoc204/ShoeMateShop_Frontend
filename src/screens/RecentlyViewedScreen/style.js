import {StyleSheet} from 'react-native';
import {colors} from '../../constants/colors';

const st = StyleSheet.create({
  text: {
    fontSize: 24,
    color: colors.background_onboard,
  },
  img: {
    width: 100,
    height: 100,
  },
});

export default st;
